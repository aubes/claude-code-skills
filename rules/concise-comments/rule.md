When writing code comments:

- Be concise: one short sentence max, no filler words
- Never explain how the framework or language works
- Only mention framework/language behavior when it justifies an architectural decision or a non-obvious choice
- If the code is self-explanatory, don't add a comment at all
